# 로그인 차단 및 계정 매칭 사고 — 2026-08-08

## 결론

Google 인증 성공과 게임 데이터 준비를 하나의 성공 조건처럼 묶은 것이 로그인·로비 진입 장애의 원인이다. Google 계정 인증으로 사용자 `uid`가 확인되면 로그인은 완료다. Graphics Studio 데이터, 플레이어 진행도, revision 읽기는 그 결과를 취소하거나 게임 진입을 막을 권한이 없다.

## 오늘 확인한 정상 계정 상태

- 최고관리자 Google 계정: `zard5388@gmail.com`
- 표시 이름: `정실장`
- 골드: `205`
- Stage 1: 클리어, 최고 `12:46`
- Stage 2: 클리어, 최고 `6:06`
- Stage 3: 클리어, 최고 `5:21`
- Stage 4: 클리어, 최고 `8:01`
- Stage 1~4 모두 입장 가능

이 상태와 다른 빈 이름, 빈 기록, 초기 골드, 잠긴 Stage 2~4가 로그인 뒤 계속 표시되면 치명적인 계정 매칭 버그다. 이 수치는 코드에 심는 기본값이 아니라 실제 계정 정본 연결을 검증하기 위해 오늘 화면에서 관측한 기준이다.

## 실제 장애 경로

1. 사용자가 게임 시작을 누른다.
2. Google 로그인은 성공하고 Firebase Authentication 사용자 `uid`가 나온다.
3. 과거 코드는 이어서 Graphics Studio 원격 상태와 플레이어 진행도를 읽었다.
4. 이 추가 읽기 중 하나가 실패하거나 늦으면 로그인 성공 뒤에도 오류를 표시하거나 로비 진입을 중단했다.
5. 진입 차단을 제거한 뒤에는 로비가 먼저 열렸지만, `Lobby.jsx`가 마운트 순간의 빈 기록을 `useState(loadPlayerRecords)`로 한 번만 읽었다.
6. 진행정보가 뒤늦게 정상 적용돼도 로비 기록 state가 갱신되지 않아 Stage 2~4가 잠긴 화면이 유지될 수 있었다.

## Git 이력 근거

| 커밋 | 실제 영향 |
| --- | --- |
| `b029fdac0418ef354c7a04ec497adae497f01324` | App에 Firebase 진행도 준비 전 전체 게임 부트스트랩 차단과 TitleScreen의 Studio 준비 확인 연결을 도입했다. |
| `a9c4bd698dbd870ab10ce4173e52cd50a287a0f3` | Studio 읽기 실패 시 타이틀에 그래픽 오류를 표시하고 시작을 중단하는 경로를 명시했다. |
| `392cb41524e5d488888b44508f336d4cdc6627df` | 로그인 뒤 동의·닉네임 흐름을 추가하면서 기존 Studio 준비 게이트를 유지했다. |
| `6ef2262a0a1841a4b7824f8c8bee20d462c1a106` | Studio 확인 뒤 플레이어 진행도 hydrate를 추가로 기다리고 실패 시 중단했으며, 로그인 시작 시 타이틀 BGM을 강제로 정지했다. |
| `b0111b1f5ef0d1aa9a480cfd2d6b49ab0c014d67` | 웹 인증을 브라우저 영구 지속성으로 바꿔 새 페이지에서도 이전 세션이 자동 복원되게 했다. |

`6ef2262`가 Studio 게이트를 최초로 만든 것은 아니다. Studio 게이트는 그보다 앞선 이력에 있었고, `6ef2262`는 진행도 게이트와 BGM 로그인 결합을 추가했다. 커밋의 Author/Committer는 저장소 공용 자격증명 `JungGit-zard <zard5388@gmail.com>`로 기록돼 있다. 이것만으로 실제 작업한 사람이나 에이전트를 특정할 수 없으며, `6ef2262` 메시에는 별도 Co-Authored-By가 없다.

## 영구 불변조건

1. Google 인증 성공으로 `uid`가 나오면 즉시 로그인 완료다.
2. Studio·진행도·revision 읽기는 로그인 성공을 실패로 바꾸지 않는다.
3. 일반 게임 주소는 데이터 읽기 실패 때문에 막히지 않는다.
4. 진행정보가 늦게 도착하면 열린 로비가 즉시 다시 읽고 스테이지 상태를 복원한다.
5. `zard5388@gmail.com`만 Graphics Studio 최고관리자다.
6. 타이틀 BGM은 로그인/로그아웃 상태 변화만으로 정지하지 않는다.
7. Firebase 실제 계정 데이터를 테스트 기본값으로 덮어쓰거나 초기화하지 않는다.

## 2026-08-08 수정

- `TitleScreen`에서 게임 시작과 Studio 준비 callback/error/retry UI 결합을 제거했다.
- `ReadyGameApp`과 `App`의 일반 게임 시작 경로에서 해당 callback 전달을 제거했다.
- `6ef2262`가 넣은 BGM 로그인 정지 결합을 제거했다.
- Firebase Auth는 브라우저 local/session persistence를 선택하지 않고 메모리 persistence를 사용한다.
- `Lobby`는 인증 사용자의 진행 상태가 `ready`로 바뀌는 순간 기록을 다시 읽어 열린 스테이지를 즉시 복원한다.
- 실제 Firebase 원격 계정 값은 변경·초기화하지 않았다.

## 치명적 버그 판정

다음은 모두 완료 불가 수준의 치명적 버그다.

- Google 인증 성공 후 Firebase 데이터 오류 때문에 로그인 실패로 표시
- Google 인증 성공 후 로비 진입 차단
- `zard5388@gmail.com`을 다른 게임 계정과 연결
- 진행정보 도착 전 빈값을 영구 화면 상태로 고정
- 기존 Stage 1~4 클리어 기록을 잠금 상태로 표시
- 이름 없는 생존자 또는 임의 기본 계정으로 대체
- 로그인 상태 변화만으로 타이틀 BGM 정지
