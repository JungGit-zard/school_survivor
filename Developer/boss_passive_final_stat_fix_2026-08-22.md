# 보스 패시브 최종 능력치 보정

일자: 2026-08-22

- B01 삼각자와 B02 복도 출입증은 대상 무기의 런중 공격력 카드 증가분에도 기존 `bossPassiveDamageMultiplier`를 적용한다.
- B04 급식 국자 해금 뒤 `maxHealth` 카드는 최대 HP와 회복량을 각각 `20 × 1.05 = 21` 올린다. 미해금 상태는 기존 +20을 유지한다.
- B03 체육관 호루라기, 보스 처치 연결, Firebase 저장·복원, HUD 8슬롯은 변경하지 않았다.

검증:

- RED: 기존 구현에서 B01/B02 여섯 대상의 카드 증가분과 B04 +21 검증이 모두 실패했다.
- GREEN: `vitest` 집중 4파일 51개 테스트 통과.
- `git diff --check` 통과.
