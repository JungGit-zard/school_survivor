# Student Lantern Half Range And Arm Pose - 2026-07-04

- Added `lanternAim` to the existing player arm action system.
- `StudentLanternWeapon` refreshes `lanternAim` while the beam is lit, so the right arm stays raised forward during the attack.
- Reduced `studentLantern` catalog cone size from `5.2 x 3.6` to `2.6 x 1.8`.
- Updated StudentLantern fallback render and damage values to the same half-range defaults.

