# threemini routing trail — StunGun 2차·3차 visual identity (2026-07-26)

- 전문 영역: R3F 전기 연쇄 시각 endpoint.
- 후속 bolt와 arc는 선택된 target의 동일한 `{ rb, generation }`을 쓴다.
- 재사용된 이전 generation은 시각 endpoint로 추적하지 않는다.
- 모델·Studio transform·Firebase는 변경하지 않는다.
- 이 기록은 routing 증적이며 외부 Kanban 실행을 주장하지 않는다.
