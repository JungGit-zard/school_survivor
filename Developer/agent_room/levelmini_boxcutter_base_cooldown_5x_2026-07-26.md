# levelmini 라우팅: 커터칼 기본 쿨다운 5배

## 라우팅 판단

- 보드: `escape-zombie-school`
- 전문 영역: 게임플레이 무기 기본 수치·밸런스
- 대상 프로필: `levelmini`

## 확정 적용

업그레이드 없는 커터칼 기본 쿨다운만 `650ms`에서 `3250ms (3.25초)`로 5배 늘린다. 피해, 사거리, 폭, 넉백, 치명타와 기존 업그레이드 정의는 유지한다.

## 완료 기준

정본 카탈로그와 단위 테스트가 `3250` 및 `650 * 5`를 함께 보장한다.
