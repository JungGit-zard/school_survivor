# 모바일 로비 터치 대상 보정 (2026-07-31)

## 변경 범위

- `Developer/r3f_prototype/src/components/Lobby.jsx`의 스테이지 카드 내부 제어 버튼만 수정했다.
- `입장하기`(잠김/준비 중 disabled 상태 포함)의 `minHeight`를 42px에서 44px으로 올렸다.
- `점수 레코드`(disabled 상태 포함)의 `minHeight`를 30px에서 44px으로 올렸다.
- 설정 버튼 44px, 하단 메뉴 버튼 48px, 카드/프리뷰 구조와 버튼 이름·ARIA 계약은 변경하지 않았다.

## 레이아웃 확인

- 프리뷰 높이는 144px이다.
- 점수 레코드: 상단 34px, 높이 44px → 세로 범위 34–78px.
- 입장하기: 하단 10px, 높이 44px → 세로 범위 90–134px.
- 두 제어 버튼 사이에는 카드 내부에 12px 간격이 남는다. 카드 외부 높이나 하단 메뉴를 늘리지 않아 320/390/412px 모바일 및 1280px 데스크톱에서 다음 카드·하단 메뉴와 겹치는 변경이 없다.

## 제외 사항

- Firebase, Graphics Studio, 인증, localStorage 및 저장 경로는 변경하거나 접근하지 않았다.
