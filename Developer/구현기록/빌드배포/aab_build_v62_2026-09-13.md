# v62 AAB 빌드 기록

- 빌드 시각: 2026-09-13 18:36 KST
- 소스 HEAD: `6f7872a486830a551d6ac0b493cae5d7a5342e5e`
- 패키지: `com.jungyoon.zombieschool`
- 버전: `versionCode 62`, `versionName 1.0.33`
- 불변 보관본: `Developer/r3f_prototype/android/app/build/outputs/bundle/release/app-release-v62-20260913_1836-6f7872a48683.aab`
- 크기: `15,627,427` bytes
- SHA-256: `9a9ade0caa8182b31b4e0e7aa87b36abaa1fc7fae8411e2d534a68694307c8d6`

## 빌드 및 정적 게이트

- `node scripts/assert-firebase-release-env.mjs`: PASS
- `npm run build`: PASS. Studio-게임 동기화 테스트 `41/41` 통과.
- `npm.cmd exec -- cap sync android`: PASS. `@capacitor-firebase/authentication@8.3.0` Android 플러그인 반영 확인.
- Android Studio JBR로 `gradlew.bat bundleRelease`: AAB 생성 확인.
- bundletool `validate`: exit `0`.
- bundletool manifest: `versionCode=62`, `versionName=1.0.33`, 패키지 일치.
- `jarsigner -verify -certs`: exit `0`.

## Firebase 및 로그인 연결 확인

- `.env`의 필수 `VITE_FIREBASE_*` 키, Firebase 프로젝트/도메인, Android package/release SHA-1 게이트: PASS.
- AAB 안 `firebaseAuth` 번들에 Firebase project, auth domain, RTDB host, API key 존재를 확인했다. API key 원문은 기록하지 않는다.
- `default_web_client_id` Android 리소스 존재, Capacitor Firebase Authentication 플러그인 존재를 확인했다.
- 생산 AAB `firebaseAuth` 번들에 `browserLocalPersistence` 문자열이 없다.
- AAB 안 타이틀 BGM SHA-256은 잠금 정본 `991bf9871fe70b55852920390b3b1434892cfc50da79d3e8fd900062b191cffe`와 일치한다.

## 미수행 항목

- `adb devices` 결과 연결 기기 없음.
- 기기 update 설치, 앱 실행, Google 계정 선택, Firebase Auth 사용자 반영, 로비 진입은 수행하지 않았다.
- Play Console 업로드도 수행하지 않았다.
