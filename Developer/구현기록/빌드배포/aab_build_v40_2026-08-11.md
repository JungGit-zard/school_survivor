# AAB Build v40 — 2026-08-11

- 소스 커밋: `9a94d891da052b562af5a59397469823e9842459` (`zombie_only`).
- 격리 소스 아님 — 공유 워크트리에서 미커밋 변경을 포함한 채 빌드했다. 포함된 미커밋 변경:
  - `android/app/build.gradle` — `versionCode 34 → 40`, `versionName "1.0.19" → "1.0.20"`
  - `package.json` — `prebuild`/`pretest`에 스튜디오-게임 동기화 계약 게이트 추가, `verify:studio-game-sync` 스크립트 신설
  - `src/App.jsx` — 게임 마운트 시 Firebase 정본 스튜디오 상태를 무조건 하이드레이트하는 `useEffect` 추가(스튜디오·어드민 경로 제외)
  - `src/App.firebaseBootstrap.test.jsx` — 위 변경에 맞춰 `canonicalHydrate` 기대값을 "호출 안 됨" → "`{}`로 호출됨"으로 반전
  - `src/lib/firebaseStudio.js` — `revision < 0` → `revision <= 0` (revision 0 스냅샷 거부)
  - `src/lib/firebaseStudio.test.js` — revision 0 거부 회귀 테스트 추가
  - `src/components/StageObjects/stageObjectPlacements.js` — 그래픽 스튜디오 저장 라운드트립으로 JSON 표기 재작성, 오브젝트 90 → 85
  - `scripts/assert-studio-game-sync-contract.mjs` — 신규(미추적). 스튜디오 Apply → 게임 전달 소스 계약 강제
- 산출물: `Developer/r3f_prototype/android/app/build/outputs/bundle/release/app-release-v40-20260811_0127-af329340d46c.aab`
- 바이트: `15,773,712` / SHA-256: `af329340d46c777815327891f40ef6f4b9d12948c8c9b9f920a32daac33a8df2`
- 빌드 파이프라인: `npm run build`(prebuild 게이트 + vite build + postbuild B02 아티팩트 게이트·hosting 자산 검증) → `npx cap sync android` → `JAVA_HOME=Android Studio jbr` + `gradlew.bat clean :app:bundleRelease`. launchmini 보고 기준 전 게이트 통과.
- 매니페스트 검증: `app/build/intermediates/bundle_manifest/release/processApplicationManifestReleaseForBundle/AndroidManifest.xml` 문자열 확인 —
  `package="com.jungyoon.zombieschool"`, `versionCode="40"`, `versionName="1.0.20"`. (bundletool 미사용. AAB 내부 매니페스트는 protobuf라 직접 문자열 확인이 불가해 병합 매니페스트로 대체 확인했다.)
- 서명 검증: `jarsigner -verify` → `jar verified.` 통과. PKIX 경고는 자체 서명 업로드 키스토어의 정상 출력이다.

## 검증하지 않은 항목

- Play Console 업로드 하지 않음. 커밋·푸시 하지 않음.
- 전체 Vitest 미완주. 빌드 게이트(prebuild/postbuild)만 통과했다. 다른 에이전트 작업으로 사전 존재하는 실패 스위트: `firebaseProgress.js`의 `normalizeZombieEncounterMap is not defined`, burstEvents, waveTimelines, enemySimulation.parity, studentProximity, projectAdminRules, GraphicsStudio.test.jsx.
- 안드로이드 실기기/AVD 구동, 네이티브 구글 로그인, 타이틀 시각 패리티, 로비·게임플레이, 일시정지·복귀 전부 미검증.
- 스테이지 프롭 겹침 정리 미반영. 게임은 Firebase `propPlacements`를 `stageObjectPlacements.js` 하드코딩보다 우선하므로(`src/lib/stagePropPlacements.js:149` `getStagePropOverride`), 그래픽 스튜디오에서 정리 후 Apply 하면 이 AAB를 다시 빌드하지 않아도 런타임에 반영된다.
