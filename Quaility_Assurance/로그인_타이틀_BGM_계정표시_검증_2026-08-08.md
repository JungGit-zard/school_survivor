# 로그인 타이틀 BGM 계정표시 검증 — 2026-08-08

## 치명적 계정 매칭 회귀 기준

- `zard5388@gmail.com` 로그인 뒤 표시 이름 `정실장`, 골드 `205`, Stage 1 `12:46`, Stage 2 `6:06`, Stage 3 `5:21`, Stage 4 `8:01`, 전 스테이지 입장 가능 상태가 실제 화면에 복원돼야 한다.
- 위 값은 테스트에 주입할 기본값이 아니라 실제 계정 정본 연결을 확인하기 위한 관측 기준이다.
- 진행정보가 로비 마운트보다 늦게 도착해도 같은 화면에서 기록과 잠금 상태가 갱신돼야 한다.
- 빈 기록 또는 잠긴 Stage 2~4가 유지되면 즉시 치명적 계정 매칭 회귀로 판정한다.

## 검증 대상

- 프로젝트: Escape! zombie school
- 작업 브랜치: `zombie_only`
- 검증 HEAD: `36ec0693a2fc1a90da65286523c7ad52168359ab`
- 관련 커밋: `6ef2262 fix: align legal consent and settings UI`
- 검증 목적: `6ef2262` 계열에서 들어온 타이틀 BGM의 Google 로그인/계정 상태 전환 pause coupling 제거 여부를 확인하되, 타이틀 BGM 정본 파일 bytes 및 재생 asset path는 바뀌지 않았음을 증명한다.

## 정본 잠금 조건

프로젝트 정책상 아래 타이틀 BGM 정본은 삭제·교체·변환·재생 경로 변경 금지 대상이다.

- 정본 경로: `Developer/r3f_prototype/src/assets/audio/title_bgm.m4a`
- 정본 bytes: `998122`
- 정본 SHA-256: `991bf9871fe70b55852920390b3b1434892cfc50da79d3e8fd900062b191cffe`

## 실제 확인 명령 및 결과

### 1) Git 상태 및 검증 기준

명령:

```bash
git status --short --branch
git rev-parse --show-toplevel
git show --stat --oneline --decorate 6ef2262 -- Developer/r3f_prototype/src/components/TitleScreen.jsx Developer/r3f_prototype/src/components/TitleScreen.bgm.test.jsx Developer/r3f_prototype/src/assets/audio/title_bgm.m4a
```

결과 요약:

- 현재 브랜치: `zombie_only...origin/zombie_only`
- 현재 작업트리에는 여러 선행 변경이 존재한다.
- `6ef2262`는 `Developer/r3f_prototype/src/components/TitleScreen.jsx`만 변경했고, `Developer/r3f_prototype/src/assets/audio/title_bgm.m4a`는 변경 대상이 아니었다.
- 현재 검증 범위에서 `title_bgm.m4a` 자체의 git diff는 없다.

### 2) 타이틀 BGM 정본 bytes/SHA 확인

명령:

```bash
wc -c Developer/r3f_prototype/src/assets/audio/title_bgm.m4a
sha256sum Developer/r3f_prototype/src/assets/audio/title_bgm.m4a
git status --short -- Developer/r3f_prototype/src/assets/audio/title_bgm.m4a
```

실제 결과:

```text
998122 Developer/r3f_prototype/src/assets/audio/title_bgm.m4a
991bf9871fe70b55852920390b3b1434892cfc50da79d3e8fd900062b191cffe *Developer/r3f_prototype/src/assets/audio/title_bgm.m4a
```

판정:

- bytes 일치: PASS
- SHA-256 일치: PASS
- asset 파일 변경 없음: PASS

### 3) canonical title BGM gate

명령:

```bash
npm run verify:title-bgm
```

실제 결과:

```text
> escape-zombie-school-r3f@0.1.0 verify:title-bgm
> node scripts/assert-title-bgm-canonical.mjs

Canonical title BGM source gate passed.
```

판정:

- 정본 파일 bytes/SHA 검증: PASS
- `TitleScreen.jsx`의 재생 import path 검증: PASS
- `audioDiagnostics.js`의 재생/진단 import path 검증: PASS
- provenance manifest의 titleBgm metadata 검증: PASS

### 4) 재생 asset path 확인

관련 소스 확인 결과:

- `Developer/r3f_prototype/src/components/TitleScreen.jsx` line 20:
  - `import titleBgmUrl from '../assets/audio/title_bgm.m4a'`
- `Developer/r3f_prototype/src/components/TitleScreen.jsx` line 201~206:
  - `new Howl({ src: [titleBgmUrl], loop: true, html5: false, preload: false, volume: 0.5, ... })`
- `Developer/r3f_prototype/src/lib/audioDiagnostics.js` line 2:
  - `import titleBgmUrl from '../assets/audio/title_bgm.m4a'`

판정:

- 타이틀 BGM 재생 asset path는 기존 정본 m4a 경로 그대로다: PASS
- wav 대체 또는 새 BGM path 전환은 발견되지 않았다: PASS

### 5) sign-in pause coupling 제거 확인

검색/소스 확인 결과:

- `TitleScreen.jsx`에서 제거된 결합 요소:
  - `signingIn` 구독 제거
  - `titleBgmControllerRef` 제거
  - `suspendedForSignIn` 제거
  - `suspendForSignIn()` 제거
  - `useLayoutEffect(() => { if (signingIn) ... }, [signingIn])` 제거
- 남아 있는 `audio.pause()`는 타이틀 언마운트 cleanup 경로에만 존재한다.
  - 위치: `Developer/r3f_prototype/src/components/TitleScreen.jsx` line 279~289
  - 의미: 게임 시작/로비 이동 등으로 TitleScreen이 unmount될 때 정리
- 신규/수정 테스트:
  - `Developer/r3f_prototype/src/components/TitleScreen.bgm.test.jsx`
  - 테스트명: `keeps the title BGM playing while sign-in state changes`
  - 검증 내용: `useAuthStore.setState({ signingIn: true })` 및 `signingIn: false` 전환 중 `audio.pause`가 호출되지 않고, unmount 때만 1회 호출된다.

판정:

- 기능 코드 기준으로 sign-in 상태 전환이 타이틀 BGM pause를 직접 유발하는 결합은 제거됨: PASS

### 6) BGM 전용 테스트

명령:

```bash
npx vitest run src/components/TitleScreen.bgm.test.jsx
```

실제 결과:

```text
RUN  v4.1.6 D:/JungSil/2.Minigame_project/school_survivor-integration/Developer/r3f_prototype

 Test Files  1 passed (1)
      Tests  4 passed (4)
   Start at  00:38:01
   Duration  2.29s (transform 537ms, setup 219ms, import 560ms, tests 321ms, environment 1.01s)
```

판정:

- BGM 동작 단위 테스트: PASS
- sign-in state change 중 pause 금지 회귀 테스트: PASS

## 발견된 주의사항 / 차단 리스크

### A. 전체 `npm test -- TitleScreen.bgm.test.jsx`는 pretest에서 차단됨

명령:

```bash
npm test -- TitleScreen.bgm.test.jsx
```

실제 결과:

```text
> escape-zombie-school-r3f@0.1.0 pretest
> npm run branch:check && node scripts/assert-title-surface-canonical.mjs && node scripts/assert-title-bgm-canonical.mjs && node scripts/assert-no-legacy-b02.mjs

branch guard: ok
Title surface canonical gate failed:
- SHA-256 mismatch: src/components/TitleScreen.jsx
```

의미:

- BGM 정본 gate는 통과하지만, 전체 npm test 진입 전 `assert-title-surface-canonical.mjs`가 현재 `TitleScreen.jsx` SHA mismatch로 차단한다.
- 따라서 release/build/test pipeline 관점에서는 title surface canonical manifest 갱신 또는 해당 변경 승인 기록이 필요하다.
- 본 검증은 `npx vitest run src/components/TitleScreen.bgm.test.jsx`로 BGM 테스트 자체를 직접 실행해 PASS를 확인했다.

### B. 주석 잔여 불일치

현재 `TitleScreen.jsx` line 196~197에는 아래 의미의 과거 주석이 남아 있다.

```text
로그인 시작 시에는 BGM을 즉시 멈추며 ... 자동 재시도하지 않는다.
```

하지만 실제 코드와 테스트는 sign-in 중 BGM을 멈추지 않는 방향으로 바뀌었다. 기능 결합은 제거됐지만, 주석은 현재 구현과 반대로 읽힌다. 릴리스 전 작은 문서/주석 정리가 권장된다.

또한 line 278의 `브라우저 Audio 정지 실패는 재생 재시도 없이 무시한다.` 주석은 제거된 sign-in suspend 블록의 잔여 주석처럼 보이며, cleanup 흐름 앞에 어색하게 남아 있다.

## 최종 판정

- 타이틀 BGM 정본 파일 bytes/SHA 보존: PASS
- 타이틀 BGM 재생 asset path 보존: PASS
- sign-in state change가 BGM pause를 직접 호출하지 않도록 decoupling됨: PASS
- BGM 회귀 테스트 직접 실행: PASS, 4/4
- 전체 `npm test -- TitleScreen.bgm.test.jsx`: BLOCKED by title surface canonical SHA mismatch, BGM gate 자체 실패는 아님

최종 결론: 오디오 정본과 BGM 동작 기준으로는 6ef2262의 title-BGM sign-in pause coupling 제거가 검증됐다. 다만 현재 작업트리는 `TitleScreen.jsx` title surface canonical gate가 막고, 과거 주석 2곳이 구현과 어긋나므로 release/merge 전 별도 승인 또는 정리가 필요하다.
