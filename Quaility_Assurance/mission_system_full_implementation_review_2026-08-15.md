# 미션 전체 구현 단일 정적 검토 — 2026-08-15

- 판정: 조건부 완료
- 확인: 로비 하단 기존 3개 뒤 네 번째 미션 버튼, 30개 카탈로그와 30개 이벤트 경로, HUD/결과 요약, Firebase Auth `uid` 기반 `users/{uid}/progress/missions` 저장 경계가 연결돼 있다.
- 확인: 전투 루프는 메모리 집계만 하며 Firebase 쓰기는 런 종료 배치에 한정된다. 풀/특수 적 사망 중복 키와 실제 텀블러·종 적중 경로가 있다.
- 보존: 이번 미션 변경에서 타이틀, Studio, 오디오, 스테이지 수치·스폰·240초, Stage 1 E04 제외 설정을 변경하지 않았다.
- 승인/활성화: 사용자가 30개 미션 골드 보상액을 확정해 `MISSION_REWARD_APPROVED=true`로 전환됐다. 완료·미수령 상태만 48px 이상 `받기` 버튼을 활성화하고, 기존 Firebase 원자 transaction·claimLedger 경로로 지급한다. 성공 시 `받음`, 실패 시 비차단 안내를 표시한다.
- 사용자 지시에 따라 테스트·빌드·브라우저 및 반복 검토는 실행하지 않았다.
