# Stage 4 가로 절반 그래픽 배치 기록

- 급식실 세로 비율과 회색 반사 타일 재질은 유지한다.
- 가로 실제 범위만 `28.8→14.4 units`로 줄인다.
- 타일 1칸은 `0.8×0.8 units`로 유지하여 가로 반복만 `36→18`로 줄인다.
- 소품은 모델·재질·회전·크기를 변경하지 않고 X 0.5배 배치를 기준으로 새 벽에 맞게 재배치한다. 벽면 소품은 관통·사이 틈이 없는 범위에서 X만 최소 조정한다.
- 압력 가마솟의 중앙 랜드마크 위치와 0.2 모델 기본 크기는 유지한다.
