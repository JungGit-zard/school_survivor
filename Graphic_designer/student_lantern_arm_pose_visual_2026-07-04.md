# Student Lantern Arm Pose Visual - 2026-07-04

- Visual intent: the player raises the right arm forward while the lantern beam is active.
- Reused the existing 3D toon player arm rotation pipeline instead of adding a separate model or prop system.
- Beam visual now uses the smaller half-range cone so the displayed light matches the attack area.

