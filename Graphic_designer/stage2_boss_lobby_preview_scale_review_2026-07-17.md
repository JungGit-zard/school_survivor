# Stage 2 보스 크기 복구 검토 - 2026-07-17

## 요청

- 게임 로비/스테이지 선택 화면에서 Stage 2 보스가 Stage 1 보스보다 과하게 크게 보인다.
- 타이틀 화면에서도 Stage 2 보스가 두 배 가까이 크게 보인다.
- Stage 1 보스 정도의 체감 크기로 복구한다.

## 그래픽 판단

- `Graphic_designer/Bang_survivor_Graphic_concept.md`의 three.js 카툰 렌더링/외곽선 방향은 유지한다.
- 보스 모델 자체, 카툰 재질, 외곽선, 전투용 스케일은 변경하지 않는다.
- 원인은 단순 카드 프레이밍만이 아니라 `zombie-b02-teacher` Graphics Studio 루트 스케일 튜닝이 런타임 `ZombieMesh` 경로에 그대로 적용되는 구조로 판단했다.
- 그래서 로비 카드, 타이틀, 실제 B02 표시 경로가 함께 커질 수 있다.

## 적용 방향

- Stage 2 보스 `B02`의 저장된 루트 크기 축을 정본 기본값으로 복구한다.
- 로비/스튜디오 Stage Boss Preview는 루트 스케일 복구 뒤 크라운 여백만 보정한다.
- Stage 1 보스 `B01`의 기본 프리뷰 크기는 유지한다.
- 전투 중 보스 크기, 체력, 충돌, 공격 패턴은 변경하지 않는다.
